[project]
name = "MoveMatch"
version = "0.1.0"
description = "Watch the sequence. Reproduce the moves. Let AI decide who got it right."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]